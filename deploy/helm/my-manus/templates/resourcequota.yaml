{{- if .Values.resourceQuota.enabled }}
apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ include "my-manus.fullname" . }}-sandbox
spec:
  hard:
    pods: {{ .Values.resourceQuota.maxSandboxPods | quote }}
    requests.memory: {{ .Values.resourceQuota.requestsMemory | quote }}
{{- end }}

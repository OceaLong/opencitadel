apiVersion: v1
kind: Secret
metadata:
  name: {{ include "opencitadel.fullname" . }}-postgres-bootstrap-secret
  labels:
    app.kubernetes.io/component: postgres
type: Opaque
stringData:
  POSTGRES_ADMIN_PASSWORD: {{ .Values.secrets.postgresAdminPassword | quote }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "opencitadel.fullname" . }}-execution-kernel-metrics
spec:
  selector:
    app.kubernetes.io/component: execution-kernel
  ports:
    - name: metrics
      port: {{ .Values.executionKernel.metricsPort }}
      targetPort: metrics

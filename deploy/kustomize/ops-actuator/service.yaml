apiVersion: v1
kind: Service
metadata:
  name: opencitadel-ops-actuator
spec:
  selector:
    app.kubernetes.io/name: opencitadel-ops-actuator
  ports:
    - name: mcp
      port: 8091
      targetPort: mcp

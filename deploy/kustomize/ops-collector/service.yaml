apiVersion: v1
kind: Service
metadata:
  name: opencitadel-ops-collector
spec:
  selector:
    app.kubernetes.io/name: opencitadel-ops-collector
  ports:
    - name: mcp
      port: 8090
      targetPort: mcp

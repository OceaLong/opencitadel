/** Max zip upload size for code knowledge base (must match nginx client_max_body_size). */
export const CODEBASE_ZIP_MAX_BYTES = 200 * 1024 * 1024;

/** Default timeout for file uploads (5 minutes). */
export const FILE_UPLOAD_TIMEOUT_MS = 5 * 60 * 1000;

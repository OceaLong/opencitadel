[project]
name = "opencitadel-ops-actuator"
version = "0.1.0"
description = "Bounded, registered-write MCP actions for OpenCitadel Ops Patrol remediation"
requires-python = ">=3.12,<3.13"
dependencies = [
  "kubernetes>=31.0.0",
  "mcp>=1.22.0,<2",
  "pydantic>=2.12.5",
  "pydantic-settings>=2.12.0",
]

[project.scripts]
opencitadel-ops-actuator = "opencitadel_ops_actuator.main:main"

[dependency-groups]
dev = ["pytest>=8.4.2", "pytest-asyncio>=1.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/opencitadel_ops_actuator"]

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]

{
  "name": "opencitadel-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:list": "playwright test --list",
    "test:meta": "playwright test --config meta/playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
